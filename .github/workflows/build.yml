name: Build, test, publish

on: [push, pull_request]

jobs:
  build:
    name: Test
    runs-on: ubuntu-18.04
    steps:
    - uses: actions/checkout@v2
# REENABLE - disabled for speed...
#    - name: test
#      run: make test
# DELETE - kept for reference
#    - name: build
#      run: fastlane build flavor:unbranded
    - name: build
      uses: maierj/fastlane-action@v1.4.0
      with:
        lane: build
        flavor: unbranded
